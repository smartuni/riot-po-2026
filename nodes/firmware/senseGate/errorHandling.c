#include "include/errorHandling.h"

int err = 0;

int door_health_check(void)
{   
    // // check current Sensor 
    // int sensor_value = gpio_read(doorSensor);  
    // // Check if sensor value ist valid
    // if (sensor_value != 0 && sensor_value != 1) {
    //     puts("Error: Invalid sensor value. Must be 0 or 1.");
    //     return err | = INVALID_DOOR_STATUS;
    // }

    return err;
}